# cbindgen configuration. Generates include/flexaudio.h from the C ABI of flexaudio-ffi.
# Generation command: cbindgen --config cbindgen.toml --crate flexaudio-ffi --output include/flexaudio.h

language = "C"
pragma_once = true
include_guard = "FLEXAUDIO_H"
# Emit the Rust doc comments into the C header as docs too.
documentation = true
documentation_style = "c99"
# Insert blank lines before functions for readability.
line_length = 100
tab_width = 4
cpp_compat = false

header = """
/*
 * flexaudio C API — pull-based audio capture bindings.
 *
 * This header is generated from crates/flexaudio-ffi by cbindgen. Do not edit by hand;
 * regenerate it after changing the Rust ABI.
 */
"""

[export]
prefix = ""

[enum]
# Emit C enums as `enum { ... }`, matching the #[repr(C)] discriminant values.
prefix_with_name = false
rename_variants = "QualifiedScreamingSnakeCase"

[parse]
parse_deps = false
